#
# This is in it's own file even though it too is an extern, simply
# because the interface is so big.
#

export extern {
  struct Canvas {
  }

  method setFillStyle(c : Canvas, r : float, g : float, b : float)
  method fillRect(c : Canvas, x : float, y : float, w : float, h : float)
}
