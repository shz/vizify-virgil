#
# Vizify platform definition.  All externed structs, functions, methods,
# etc. must be defined by the host platform.
#

extern {
  struct Color {
    r : float
    g : float
    b : float
    a : float
  }

  struct Canvas {
    width : int
    height : int
    fontSize : int
    fontFamily : str
    globalAlpha : float
  }

  # Stub
  function importData<T>(json : str) : T
}

